// import sendServer from '../server/myserver';
import sendServer from '../server/myserver2';
var obj = {
	name:'url'
    <!-- ****:'/healthclaim/claims/****' -->
}
sendServer.parseRouter('index',obj)
export default sendServer;
